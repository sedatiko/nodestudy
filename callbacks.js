var handleResults = function(error, results){
    // if error is undefined
    // do something with the results
}

// use anon as callback when a simple, one-time call is made
getStuff(function(err, stuffToGet){

});